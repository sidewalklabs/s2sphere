test:
  override:
    - "deactivate && pyenv shell 2.7.11 && python -V && pip install .[tests] && flake8 && nosetests -vv --exclude=compare_implementations_test"
    - "deactivate && pyenv shell 3.4.4 && python -V && pip install .[tests] && flake8 && nosetests -vv --exclude=compare_implementations_test"
    - "deactivate && pyenv shell 3.5.1 && python -V && pip install .[tests] && flake8 && nosetests -vv --exclude=compare_implementations_test"
